#ifndef CALCULATE_PRIME_NUMBERS_HPP
#define CALCULATE_PRIME_NUMBERS_HPP

#include <cmath>
#include <vector>
#include <iostream>

namespace prime {
  
int naive_prime_count(int A, int B);

int eratosthenes_prime_count(int A, int B);
  
} // namespace prime


#endif // #ifndef CALCULATE_PRIME_NUMBERS_HPP